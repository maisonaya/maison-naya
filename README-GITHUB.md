# Déploiement GitHub Pages

Ce projet suppose un dépôt nommé `maison-naya`.
Si votre dépôt porte un autre nom, modifiez `base` dans `vite.config.js`.

Après l'envoi sur GitHub, ouvrez Settings > Pages et choisissez GitHub Actions comme source.
